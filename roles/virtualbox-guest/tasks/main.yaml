---
- name: Install network packages
  become: true
  pacman:
    state: latest
    name:
      - virtualbox-guest-utils
      - virtualbox-guest-dkms

- name: Start network service
  become: true
  systemd:
    name: vboxservice
    state: started
    enabled: true
